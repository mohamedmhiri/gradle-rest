# Gradle getting started Rest App

- install gradle (tried sdkman)
- run
> gradle build
- run
> gradle wrapper
- build the app with **gradlew**
> ./gradlew build
- run the app with
> ./gradlew run
- that's it

